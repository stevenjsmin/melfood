<?xml version="1.0" encoding="UTF-8"?>
<config>

    <!-- COMMON CONFIGURATION -->
    <!-- * NOTE: You must define only common configuration here -->

    <!-- System properties load -->
    <env />

    <!-- Exception URLs for access checking -->
    <system-config>
    	<release-version>1.0.0</release-version>
        <acess-check-except-url>/common/auth/welcome.yum, /common/auth/login.yum, /common/auth/loginForm.yum, /common/auth/logout.yum, /common/EmailSendFrom.yum, /common/auth/Welcome.yum, /grouppurchase/groupPurchaseImageViewer.yum, /guest/common/sendQuestionMessage.yum, /common/postcode/suburbCmbx.yum, /common/getNumberOfCartItems.yum, /welcome/you.yum </acess-check-except-url>

        <login-fail-allow-cnt>5</login-fail-allow-cnt>
        <password-change-period-days>80</password-change-period-days>
        <password-minimum-length>6</password-minimum-length>
        
    </system-config>

	<!-- 시스템에서 데이터를 저장할 최상위 디렉토리를 지정한다. -->
	<data-directory>
		<dir type="local">/Users/Steven/Workspaces5/app-data/</dir>
		<dir type="dev">/local/app-data/</dir>
		<dir type="test">/local/app-data/</dir>
		<dir type="training">/local/app-data/</dir>
		<dir type="prod">/local/app-data/</dir>
	</data-directory>

</config>